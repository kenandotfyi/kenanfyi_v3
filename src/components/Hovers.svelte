<script>
    // Svelte component to render hover previews.
    // A little complicated than as I wanted it to be, but works quite well with some QoL features.
    // Such as being able to pin hovers, move them around and also automatically hide with some delay
    // after mouse leaves the hover area.
    // It also checks if a hover window is open and blocks opening that same hover window by clicking to another
    // link on the page. So for a particular page only one hover window is shown at a time.
</script>
